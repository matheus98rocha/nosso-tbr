export type AuthorDomain = {
  id: string;
  name: string;
  createdAt: string;
  totalBooks?: number;
};

export type AuthorPersistence = {
  id: string;
  name: string;
  created_at: string;
  total_books?: number;
};
